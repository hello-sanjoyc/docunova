DATABASE_URL="postgresql://postgres:postgres@localhost:5432/docunova?schema=public"
PORT=4000
HOST=0.0.0.0

# Generate a strong random secret: openssl rand -hex 64
JWT_SECRET="change-me-to-a-long-random-secret"

# Anthropic Claude API key — https://console.anthropic.com
ANTHROPIC_API_KEY="sk-ant-..."

# Where the Next.js frontend lives (used for CORS and share link URLs)
FRONTEND_URL="http://localhost:3000"

# Optional: override the local uploads folder
# UPLOADS_DIR="/var/data/docunova/uploads"
